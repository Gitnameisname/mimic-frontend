import { AdminSettingsPage } from "@/features/admin/settings/AdminSettingsPage";

export const metadata = { title: "시스템 설정 — Mimir Admin" };

export default function SettingsPage() {
  return <AdminSettingsPage />;
}
